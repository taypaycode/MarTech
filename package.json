{
  "name": "martech-journey",
  "version": "1.0.0",
  "description": "MarTech Journey Analytics Platform",
  "main": "api/index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "cd frontend && npm install && npm run build",
    "start": "vercel dev",
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "keywords": ["martech", "analytics", "marketing", "roi", "attribution"],
  "author": "MarTech Journey Team",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  }
}

